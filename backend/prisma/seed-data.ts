/**
 * Seed dùng lại đúng bộ hằng số mà runtime dùng — tránh hai nguồn sự thật.
 * Định nghĩa gốc nằm ở src/modules/config/config.defaults.ts
 */
export {
  CONFIG_DEFINITIONS,
  CONFIG_COLORS,
  TASK_TEMPLATES,
  SAMPLE_CRITERIA,
  DEFAULT_APP_SETTINGS,
  slugCode,
} from '../src/modules/config/config.defaults';
